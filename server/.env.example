# ==================== VARIABLES DE ENTORNO ====================
# Copiar este archivo a .env y llenar con los valores correctos

# Puerta del servidor
PORT=5000

# Entorno (development o production)
NODE_ENV=development

# JWT Secret (clave para firmar tokens, mínimo 32 caracteres)
JWT_SECRET=tu_clave_jwt_super_secreta_con_minimo_32_caracteres_para_produccion

# Ruta de la base de datos (opcional, usa ./database.db por defecto)
# DB_PATH=./database.db
