{
  "name": "mana-coffee-fullstack",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix ./mana_coffee/server install && npm rebuild --prefix ./mana_coffee/server sqlite3 --build-from-source && npm --prefix ./mana_coffee install && npm --prefix ./mana_coffee/server run setup-admin",
    "build": "npm --prefix ./mana_coffee run build",
    "start": "node ./mana_coffee/server/server.js"
  },
  "engines": {
     "node": ">=18.18.0 <25.2.1"
  }
}